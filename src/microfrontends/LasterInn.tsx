import NavFrontendSpinner from 'nav-frontend-spinner';
import React, { FunctionComponent } from 'react';
import './LasterInn.less';

const LasterInn: FunctionComponent = () => (
    <div className="laster-inn">
        <NavFrontendSpinner />
    </div>
);

export default LasterInn;
